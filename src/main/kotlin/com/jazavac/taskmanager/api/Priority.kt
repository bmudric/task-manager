package com.jazavac.taskmanager.api

/**
 * Enumeration of available [Process] priorities which influence TaskManager behaviour when deleting a process.
 */
enum class Priority {
    LOW, MEDIUM, HIGH
}